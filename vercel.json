{
    "builds": [{
        "src": "apis_backend/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "55mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "apis_backend/wsgi.py"
        }
    ]
}
